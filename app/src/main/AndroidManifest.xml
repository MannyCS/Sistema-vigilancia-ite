<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.prototipo_ss">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Prototipo_SS">
        <activity
            android:name=".adminform"
            android:exported="true"
            android:label="@string/title_activity_adminform"
            android:theme="@style/Theme.Prototipo_SS.NoActionBar" />
        <activity android:name=".zonas" />
        <activity android:name=".administrador" />
        <activity android:name=".registrarUsuario" />
        <activity android:name=".EscuelaMapa" />
        <activity android:name=".noticias" />
        <activity android:name=".Form" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>